default:
  paths:
    - e2e/features/**/*.feature
  tags: 'not @future-feature'
  require:
    - e2e/step-definitions/**/*.ts
  requireModule:
    - ts-node/register
  format:
    - progress-bar
    - json:cucumber-report.json
